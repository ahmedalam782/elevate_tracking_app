name: elevate_tracking_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  cached_network_image: ^3.4.1
  skeletonizer:
  dio: ^5.9.1
  easy_localization: ^3.0.8
  equatable: ^2.0.8
  firebase_analytics: ^12.1.1
  firebase_core: ^4.4.0
  firebase_crashlytics: ^5.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_cache_manager: ^3.4.1
  flutter_screenutil: ^5.9.3
  flutter_secure_storage: ^10.0.0
  flutter_svg: ^2.2.3
  gap: ^3.0.1
  geocoding: ^4.0.0
  get_it: ^9.2.0
  go_router: ^17.1.0
  google_maps_flutter: ^2.14.0
  image: ^4.7.2
  image_picker: ^1.2.1
  injectable: ^2.7.1+2
  internet_connection_checker_plus: ^2.9.1+2
  json_annotation: ^4.9.0
  location: ^8.0.1
  logger: ^2.6.2
  lottie: ^3.3.2
  package_info_plus: ^9.0.0
  path: ^1.9.1
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  pinput: ^6.0.2
  pretty_dio_logger: ^1.4.0
  retrofit: ^4.9.2
  shared_preferences: ^2.5.4
  shimmer: ^3.0.0
  toastification: ^3.0.3
  video_player: ^2.10.1

# ==============================================================
# Dev Dependencies
# ==============================================================
dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.11.0
  flutter_launcher_icons: ^0.14.4
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^2.9.1
  json_serializable: ^6.9.0
  mockito: ^5.6.3
  retrofit_generator: ^10.2.1

# ==============================================================
# Flutter
# ==============================================================
flutter:
  uses-material-design: true

  # ---------- Assets ----------
  assets:
    - assets/images/
    - assets/icons/
    - assets/json/
    - assets/animations/
    - assets/localization/ar-EG.json
    - assets/localization/en-US.json
    - assets/videos/

  # ---------- Fonts ----------
  fonts:
    - family: RobotoEnglish
      fonts:
        - asset: assets/fonts/english/Roboto-Light.ttf
          weight: 300
        - asset: assets/fonts/english/Roboto-Regular.ttf
          weight: 400
        - asset: assets/fonts/english/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/english/Roboto-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/english/Roboto-Bold.ttf
          weight: 700
    - family: RobotoArabic
      fonts:
        - asset: assets/fonts/arabic/Roboto-Light.ttf
          weight: 300
        - asset: assets/fonts/arabic/Roboto-Regular.ttf
          weight: 400
        - asset: assets/fonts/arabic/Roboto-Medium.ttf
          weight: 500
        - asset: assets/fonts/arabic/Roboto-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/arabic/Roboto-Bold.ttf
          weight: 700

# ==============================================================
# Flutter Assets Generator
# ==============================================================
# ---------- Icons (active) ----------
# flutter_assets:
#   assets_path: assets/icons
#   output_path: lib/core/theme
#   filename: app_icons.dart
#   ignore_comments: true
#   field_prefix: null
#   classname: AppIcons
# ---------- Animations (inactive) ----------
# flutter_assets:
#   assets_path: assets/animations
#   output_path: lib/core/theme
#   filename: app_animations.dart
#   ignore_comments: true
#   field_prefix: null
#   classname: AppAnimations
# ---------- JSON (inactive) ----------
# flutter_assets:
#   assets_path: assets/json
#   output_path: lib/core/theme
#   filename: app_json.dart
#   ignore_comments: true
#   field_prefix: null
#   classname: AppJson
# ---------- videos (inactive) ----------
# flutter_assets:
#   assets_path: assets/videos
#   output_path: lib/core/theme
#   filename: app_videos.dart
#   ignore_comments: true
#   field_prefix: null
#   classname: AppVideos
# ---------- Images (inactive) ----------
flutter_assets:
  assets_path: assets/images
  output_path: lib/core/theme
  filename: app_images.dart
  ignore_comments: true
  field_prefix: null
  classname: AppImages
